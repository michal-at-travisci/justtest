
install: skip
script: python ./src/hello.py

jobs:
  include:
   - os: linux #for comparison
     dist: bionic
   - os: freebsd
     script:
      - uname -a
      - kenv
      - env
   - os: freebsd
     python: "3.6"

