
install: skip

jobs:
  include:
   - os: linux #for comparison
     dist: bionic
     python: "3.6"
     script: python ./src/hello.py
   - os: freebsd
     script:
      - uname -a
      - kenv
      - env
   - os: freebsd
     python: "3.6"
     script: python ./src/hello.py

